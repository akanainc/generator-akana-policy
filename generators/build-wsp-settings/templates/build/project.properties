root.dir=.
project.dir=..

# Used for building Ant tools
#
classes.dir=${root.dir}/target/classes
lib.dir=${root.dir}/lib
project.lib.dir=${project.dir}/lib
dist.dir=${root.dir}/dist

# Only used when this build is run independently
#
third.party.dir=${project.lib.dir}/ext

debuglevel=lines,vars,source
javacdebug=on
nowarn=off
bundle.version=<%=props.bundleVersion%>
compile.version=1.8
source.version=1.8