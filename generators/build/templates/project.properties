root.dir=..
classes.dir=${root.dir}/bin
lib.dir=${root.dir}/lib
src.dir=${root.dir}/src
dist.dir=${root.dir}/bundles
deploy.dir.name=bin
deploy.dir=${root.dir}/build/${deploy.dir.name}
major.version=1
minor.version=0
micro.version=0
javacdebug=on
debuglevel=lines,vars,source
nowarn=false